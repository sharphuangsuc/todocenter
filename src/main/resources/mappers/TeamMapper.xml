<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cvte.todocenter.mapper.TeamMapper">

    <insert id="insertTeam" parameterType="com.cvte.todocenter.bean.Team">
        insert into team(team_name,is_delete)
        values (#{team_name},0)
    </insert>

    <select id="selectAll" resultType="com.cvte.todocenter.bean.Team">
        select team_id,team_name from team
        where is_delete!=1
    </select>

    <update id="deleteTeamById" parameterType="int">
        update team set is_delete=1
        where team_id=#{team_id}
    </update>

    <select id="selectTeamById" parameterType="int" resultType="com.cvte.todocenter.bean.Team">
        select team_id,team_name from team
        where team_id=#{team_id} and is_delete!=1
    </select>

    <update id="updateTeamById" parameterType="com.cvte.todocenter.bean.Team">
        update team
        <set>
            <if test="team_name!=null">
                team_name=#{team_name}
            </if>
            <if test="is_delete!=null">
                is_delete=#{is_delete}
            </if>
            where team_id=#{team_id}
        </set>
    </update>

    <select id="selectTeamByName" parameterType="java.lang.String" resultType="com.cvte.todocenter.bean.Team">
        select team_id,team_name from team
        where team_name=#{team_name} and is_delete!=1
    </select>

    <select id="selectAllDelTeam" resultType="com.cvte.todocenter.bean.Team">
        select team_id,team_name from team
        where is_delete=1
    </select>

    <update id="batchDeleteTeam" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" separator=";">
            update team
            <set>
                is_delete=1
            </set>
            where team_id=${item}
        </foreach>
    </update>

    <insert id="insertTeamUser" parameterType="java.util.List">
        insert into user_team(user_id,team_id,is_delete)
        values
        <foreach collection="list" item="item" separator=",">
             (#{item.user_id},#{item.team_id},0)
        </foreach>
    </insert>

    <update id="deleteTeamUserById" parameterType="int">
        update user_team set is_delete=1
        where user_id=#{user_id} and team_id=#{team_id}
    </update>

    <select id="selectAllTeamUser" resultType="com.cvte.todocenter.bean.User" parameterType="int">
        select user_id,user_name,email
        from user
        where user_id in
        (select user_id from user_team where team_id=#{team_id})
    </select>

</mapper>